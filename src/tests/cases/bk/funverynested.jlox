// --- Teste
fun nested(a) {
  if (a < 3) {
    if (a > 1) {
       retorna a;
    }
  }

  {
    a = a + 2;
    retorna a;
  }

  retorna -1;
}

saida nested(2);
saida nested(1);

// --- Esperado
// 2
// 3